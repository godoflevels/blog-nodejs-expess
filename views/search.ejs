<div class="author">
  <h1 class="author__heading">Search Results</h1>
</div>

<section classs="articles">
  <% if (data.length === 0) { %>
    <p>Ничего не найдено.</p>
  <% } %>

  <ul class="article-ul">
    <% data.forEach(post => { %>
      <li>
        <a href="/post/<%= post._id %>">
          <span><%= post.title %></span>
          <span class="article-list__date"><%= post.createdAt.toDateString() %></span>
        </a>
      </li>
    <% }) %>
  </ul>
</section>